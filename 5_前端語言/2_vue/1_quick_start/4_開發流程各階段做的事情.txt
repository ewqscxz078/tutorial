ref ChatGPT

第一階段：專案設定 & 依賴宣告（不部署）

	你在這個階段做的事是：

	* 決定用什麼框架、工具

		Vue 3

		TypeScript

		Vite

	* 在 package.json 宣告：

		* 用到哪些 JS 套件

		* 哪些是 runtime、哪些是 build tool

	👉 這個階段只是「定義規則」，還沒真的跑程式。


第二階段：npm install（建置前準備）

	npm install

	做的事：

		* 解析 package.json / package-lock.json

		* 把所有依賴抓到本機

		* 準備好「可編譯、可執行前端建置」的環境


第三階段（A）：開發期 → npm run dev

	npm run dev

	這是：

		* 開發專用

		* 啟動 Vite dev server

		* 有 HMR（熱更新）

		* 不會產生最終部署檔

第三階段（B）：佈署前建置 → npm run build

	npm run build

	它會：

		* 把 .vue、.ts、.css

		* 把所有 import 的依賴

		* 編譯、壓縮、tree-shaking

		* 輸出成 純靜態檔案

		通常在：
			dist/
			├─ index.html
			├─ assets/
			│  ├─ xxx.js
			│  ├─ xxx.css
			│  └─ ...

	👉 這個 dist/，才是真正要丟給 Nginx 的東西


可能會在意這點，幫你釐清：

	Node.js / npm：

		✔ 只在 開發 / CI / build 階段 需要

	上線環境（Prod）：

		❌ 不需要 Node.js

		❌ 不需要 npm

		✔ 只需要 Nginx / Apache / CDN

	這跟你以前 JSP 不同：

		* JSP：需要 Java runtime + Servlet container

		* Vue SPA：只有靜態檔


企業常見的兩種部署模式

	模式 A：純前端 Nginx
		Nginx
		 └─ /usr/share/nginx/html
			 └─ dist/*

	適合：

		* 前後端完全分離

		* API 走另一個 domain / gateway


	模式 B：前端 build → 丟進 Spring Boot
		Spring Boot
		 └─ resources/static/*
			 └─ dist/*

	適合：

		* 想共用同一個 domain

		* 避免 CORS

		* 內部系統、舊架構轉型

	這個模式在企業內網非常常見，你應該會很有親切感。


最後幫你一句定錨（很重要）

	npm 解決的是「前端依賴與建置期問題」，
	Vue/Vite 解決的是「開發體驗與打包」，
	Nginx 解決的是「部署與服務靜態內容」。



整條 Vue 3 + Vite 的工具鏈是這樣跑的：
	VS Code（編輯器）
	   ↓
	npm / pnpm（套件管理、腳本執行）
	   ↓
	Vite（dev server / build tool）
	   ↓
	編譯 .vue / .ts / css → dist/

	👉 VS Code 只是幫你寫程式，不提供 runtime 或建置能力
	👉 Node.js 才是執行這些工具的基礎 runtime

	在 VS Code 裡透過 Terminal / NPM Scripts / Tasks 去呼叫 Node/npm/Vite 來做事