ref ChatGPT


	* Vite ≠ npm create vue@latest
	* 但 npm create vue@latest 產生的 Vue 專案，預設是「用 Vite 當建置工具」。

釐清三個角色
	| 名稱                      | 本質是什麼            | 角色定位                      |
	| ------------------------- | --------------------- | ----------------------------- |
	| **Vue 3**                 | 前端框架              | 寫 UI、元件、反應式           |
	| **Vite**                  | 建置工具 / Dev Server | 編譯、打包、熱更新            |
	| **npm create vue@latest** | 專案產生器（CLI）     | 幫你「生一個」Vue + Vite 專案 |


用流程圖概念來看
	在終端機打：
		npm create vue@latest

	實際發生的是：
		npm
		 └─ 下載並執行 create-vue 這個 CLI
			  └─ 問你要不要 TypeScript / Router / Pinia...
				   └─ 產生一個「已經設定好 Vite 的 Vue 3 專案」
		👉 Vite 是「被選來當專案的建置工具」
		👉 npm create vue@latest 只是負責把這個專案骨架生出來

那 Vite 本身是什麼？
	Vite 是一個獨立的工具 / 套件，由 Vue 作者團隊開發，但它：
		* 不專屬 Vue（也能用在 React、Svelte、純 TS）
		* 安裝後是透過：
			npm run dev
			npm run build
		在背後呼叫 Vite

	以在專案裡看到這些證據：
		1.package.json
			{
			  "scripts": {
				"dev": "vite",
				"build": "vite build",
				"preview": "vite preview"
			  }
			}

		2.vite.config.ts
			import { defineConfig } from 'vite'
			import vue from '@vitejs/plugin-vue'

			export default defineConfig({
			  plugins: [vue()]
			})
			這才是 Vite 真正開始工作的地方。

類比到你熟悉的 Java 世界（關鍵理解）
	| Java 世界           | Vue / 前端世界     |
	| ------------------- | ------------------ |
	| Spring Boot         | Vue                |
	| Maven / Gradle      | Vite               |
	| start.spring.io     | npm create vue     |
	| mvn spring-boot:run | npm run dev        |

	👉 npm create vue@latest ≈ start.spring.io 幫你產專案
	👉 Vite ≈ Maven/Gradle + dev server


如果不用 npm create vue@latest，能不能用 Vite？
	可以，而且這會幫你完全分清楚角色：
		npm create vite@latest
	然後選：
		* Framework → Vue
		* Variant → TypeScript

	這時你會發現：
		* 專案一樣是 Vite
		* 但少了 Vue Router / Pinia 等「Vue 專用最佳化設定」


結論
	* Vite 是建置工具，不是 npm 指令

	* npm create vue@latest 是產生「Vue + Vite」專案的 CLI

	* Vue 3 主流開發路線 =
		Vue（框架） + Vite（建置） + TypeScript（語言）