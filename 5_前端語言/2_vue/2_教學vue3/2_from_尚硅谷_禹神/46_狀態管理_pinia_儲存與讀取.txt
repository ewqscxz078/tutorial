ref https://www.youtube.com/watch?v=P5F8CnowPvs&list=PLmOn9nNkQxJEnGM4Jf0liBcyedAtuQq-O&index=46

符合直覺的 狀態管理庫
	=> 集中式狀態管理
		vue2 用 vuex
		vue3 用 pinia(大波羅)

	類似於前端的 session
	或是後端開發的 jsp+session 方式

	1.pinia 關注分類保存資料方式
		store
			Count/count.ts 			// 對應元件放的共用資源
			LoveTalk/loveTalk.ts 	// 對應元件放的共用資源

	2. components 引入 useXXXXStore
		Count.vue
		  import {useCountStore} from '@/store/count'
		  const countStore = useCountStore()
		LoveTalk.vue
		  import {useTalkStore} from '@/store/loveTalk'
		  const talkStore = useTalkStore()

store
	count.ts
		import {defineStore} from 'pinia'

		export const useCountStore = defineStore('count',{

		  // 真正放資料的地方
		  state(){
		    return {
			  sum:6
			}
		  }
		});

	loveTalk.ts
		import {defineStore} from 'pinia'

		export const useTalkStore = defineStore('talk',{

		  // 真正放資料的地方
		  state(){
		    return {
			  talklist:[
				{id: 'sdf01', title: '今天你有點怪,哪裡怪?怪好看的!' },
				{id: 'sdf02', title: '草莓、藍莓、蔓越莓、你今天想我了沒' },
				{id: 'sdf03', title: '心裡給你留了一塊地，我的死心踏地' }
			  ]
			}
		  }
		});

components/
	Count.vue
		<template>
			<div class="count">
			  <h2>當前求和為: {{ countStore.sum }}</h2>
			  <select v-model.number="n">
			    <option value="1">1</option>
				<option value="2">2</option>
				<option value="3">3</option>
			  </select>
			  <button @click="add">加</button>
			  <button @click="minus">減</button>

			</div>
		</template>

		<script lang='ts' setup>
		  import {ref,reactive} from "vue";
		  import {useCountStore} from '@/store/count'

		  const countStore = useCountStore()
		  // 以下兩種方式都可以拿到 state中的資料
		  // console.log('@@@',countStore.sum)
		  // console.log('@@@',countStore.$state.sum)

		  /**
		  let obj = reactive({ // 封裝過的 ref 會自己自動拆包可略過加 value
		    a:1,
			b:2,
			c:ref(3)
		  })
		  let x = ref(9) // 自己定義的 ref 要自己拆包加 value
		  console.log(obj.a)
		  console.log(obj.b)
		  console.log(obj.c)
		  console.log(x.value)
		  **/

		  let n = ref(1)	// 用戶選擇的數字

		  function add(){

		  }
		  function minus(){

		  }
		</script>

		<style scoped>
		  .count {
		    backgroud-color: skyblue;
			padding: 10px;
			bordering: 10px;
			box-shadow: 0 0 10px;
		  }
		  select,button {
		    margin: 0 5px;
			height: 25px;
		  }
		</style>


	LoveTalk.vue

		<template>
			<div class="talk">
			  <button @click="getLoveTalk">獲取一句土味情話</button>
			  <ul>
			    <li v-for="talk in talkStore.talkList" :key="talk.id">{{talk.title}}</li>
			  </ul>
			</div>
		</template>

		<script lang='ts' setup>
		  import {reactive} from "vue";
		  import axios from "axios";
		  import { nanoid } from 'nanoid' // npm i nanoid
		  import {useTalkStore} from '@/store/loveTalk'

		  const talkStore = useTalkStore()

		  function getLoveTalk(){
			// 發請求，下面這寫法是連續解構賦值+重命名
		    // let {data:{content:title}} = await axios.get('https://api.uomg.com/api.rand.qinghua?format=json')
			//console.log(result.data.content)

			// 把請求回來的字符串包裝成一個對象
			// let obj = {id:nanoid(),title}
			// 放到數組中
			// talkList.unshift(obj)
		  }
		</script>

		<style scoped>
		  .talk {
		    backgroud-color: orange;
			padding: 10px;
			bordering: 10px;
			box-shadow: 0 0 10px;
		  }
		  select,button {
		    margin: 0 5px;
			height: 25px;
		  }
		</style>

main.ts
  import {createApp} from 'vue'

  import App from './App.vue'

  // 第一步 : 引入 pinia
  import {createPinia} from 'pinia'

  const app = createApp(App))

  // 第二步 : 創建 pinia
  const pinia = createPinia()

  // 第三步 : 安裝 pinia
  app.use(pinia)

  app.mount('#App')


App.vue
	<template>
		<div class="app">
		  <div>
			<Count/>
			<br>
			<LoveTalk/>
		  </div>

		</div>
	</template>

	<script lang='ts' setup>
	  import Count from './components/Count.vue'
	  import LoveTalk from './components/LoveTalk.vue'

	</script>
